modules = ["nodejs-20", "web"]
run = "cd portfolio-site && npm run build"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "cd portfolio-site && npm start"]
build = ["sh", "-c", "cd portfolio-site && npm install && npm run build"]

[workflows]
runButton = "Run"

[[workflows.workflow]]
name = "Run"
author = 39536624

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd portfolio-site && npm install && npm run dev"

[[ports]]
localPort = 3000
externalPort = 80
